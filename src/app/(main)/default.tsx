/**
 * Default for Main Route Group
 *
 * This handles cases where the parallel route slot needs a default.
 * Returns null since the main content is handled by page.tsx files.
 */
export default function Default() {
  return null;
}
